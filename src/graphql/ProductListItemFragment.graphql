fragment ProductListItem on ProductEntity {
	id
	attributes {
		name
		price
		description
		images {
			data {
				id
				attributes {
					url
					alternativeText
					width
					height
				}
			}
		}
	}
}
