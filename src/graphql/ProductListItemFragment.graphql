fragment ProductListItem on ProductEntity {
	id
	attributes {
		name
		price
		avg_rating
		description
		images {
			data {
				id
				attributes {
					url
					alternativeText
					width
					height
				}
			}
		}
	}
}
