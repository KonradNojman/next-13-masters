query CollectionGetProductListByCollectionId($collectionId: ID!) {
	collection(id: $collectionId) {
		data {
			attributes {
				products {
					data {
						...ProductListItem
					}
				}
			}
		}
	}
}
